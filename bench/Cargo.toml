[package]
name = "bench"
version.workspace = true
edition.workspace = true

[dependencies]
clap.workspace = true
serde_json.workspace = true
# TODO: Try to elminate csv and shapefile here
csv = "1.3"
rand = "^0.9"
serde = { version = "^1.0", features = ["derive"] }
shapefile = "0.7.0"
